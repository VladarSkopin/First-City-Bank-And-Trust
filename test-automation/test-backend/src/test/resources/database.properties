# PostgreSQL connection
db.url=jdbc:postgresql://localhost:5432/first_city_bank
db.username=postgres
db.password=_K2Vq*SbuT/$t*w
db.schema=public
db.driver=org.postgresql.Driver

# Connection settings
db.connection.timeout=30
db.pool.max.size=10
db.use.ssl=false
